# Collective Automaton Simulation v5.2

Дискретно-автоматная модель психологического взаимодействия коллектива агентов. Учебно-научный проект уровня МГУ (мехмат).

## 🚀 Основные возможности V5.2
- **High-Performance Core (C++)**: Полный перенос ежедневного цикла вычислений на C++ с использованием OpenMP.
- **C++ Logging**: Высокоскоростное логирование состояний и взаимодействий напрямую из C++ модуля.
- **Dynamic Sync**: Оптимизированная синхронизация между Python и C++, исключающая накладные расходы в безголовом (headless) режиме.
- **University Model**: Поддержка масштабных сценариев (1875+ агентов) с детализацией по факультетам и группам.

## 🛠 Установка и запуск

### 1. Автоматическая установка
**Для macOS/Linux:**
```bash
chmod +x install_macos.sh
./install_macos.sh
```

**Для Windows:**
```batch
install.bat
```

### 2. Сборка C++ ядра
Если вы вносили изменения в `core/src/`:
```bash
./build.sh
```

### 3. Запуск
```bash
./run.sh
```

## 📂 Структура проекта
- `main.py`: Точка входа в систему (поддержка `--scenario`, `--silent`, `--gui`).
- `model/`: Логика агентов и коллектива на Python.
- `core/src/`: Исходный код C++ ядра (`engine.cpp`, `logger.cpp`, `binding.cpp`).
- `gui/`: Графические интерфейсы (университет и общая симуляция).
- `data/scenarios/`: Файлы конфигурации сценариев JSON.
- `data/output/`: Результаты расчетов (CSV логи).

---
*Разработано в рамках научно-исследовательской работы.*
